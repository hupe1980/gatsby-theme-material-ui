import React from 'react';

import Section from './section';

export default function Features({ title, description, features }) {
  return (
    <Section title={title} description={description}>
      FEATURES
    </Section>
  );
}
